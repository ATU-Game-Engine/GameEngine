#pragma once

#include "../Debug/DebugUIContext.h"

/// @brief Draws the Constraint Creator Panel ImGui window for creating and managing physics constraints.
void DrawConstraintCreatorPanel(DebugUIContext& context);