#pragma once

#include "../Debug/DebugUIContext.h"

/// @brief Draws the Inspector Panel ImGui window for viewing and editing the selected object's properties.
void DrawInspectorPanel(DebugUIContext& context);